import { NotesClient } from "@/components/notes/notes-client";

// Notes hub: browse every highlight/note across books — synced, shared-shelf
// and this-device — with search, sorting and pagination.
export default function NotesPage() {
  return <NotesClient />;
}
